**Developer commands**

*These commands are only available to use by the developer.*

at.setdev - sets yourself as the developer user

at.set_once_monthly - Sets the current "monthly changed" channel by name

at.set_once_hourly - same as at.set_once_monthly, except for the hourly channel

at.manual_data_write - writes the `status` object to `data/status.bin` manually.